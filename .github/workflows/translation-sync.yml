# Original automation is preserved in ../upstream-workflows/translation-sync.yml.
name: Archived upstream translation-sync
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
