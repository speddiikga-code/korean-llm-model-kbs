# Original automation is preserved in ../upstream-workflows/release.yml.
name: Archived upstream release
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
