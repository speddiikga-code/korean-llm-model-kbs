# Original automation is preserved in ../upstream-workflows/ocr-review.yml.
name: Archived upstream ocr-review
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
