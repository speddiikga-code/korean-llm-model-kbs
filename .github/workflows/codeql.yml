# Original automation is preserved in ../upstream-workflows/codeql.yml.
name: Archived upstream codeql
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
