# The Korean edition source import is complete. Normal updates use deploy-pages.yml.
name: Archived initial Korean edition import
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Korean edition source is already published."
