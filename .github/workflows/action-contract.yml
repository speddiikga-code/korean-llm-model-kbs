# Original automation is preserved in ../upstream-workflows/action-contract.yml.
name: Archived upstream action-contract
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
