# Original automation is preserved in ../upstream-workflows/pages-ci.yml.
name: Archived upstream pages-ci
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Korean edition workflows."
